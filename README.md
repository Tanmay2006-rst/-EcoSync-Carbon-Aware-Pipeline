# -EcoSync-Carbon-Aware-Pipeline
An AI-powered, carbon-aware data pipeline that leverages machine learning (Random Forest) to dynamically schedule resource-heavy backend computing workloads during peak renewable energy windows.
